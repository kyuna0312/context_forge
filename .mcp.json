{
  "mcpServers": {
    "forge-db": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp/server.mjs"],
      "env": {
        "DATABASE_URL": "${FORGE_DATABASE_URL}"
      }
    }
  }
}
